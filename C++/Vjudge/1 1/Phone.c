#include<stdio.h>


struct number{

    char phone_num[100];

};


int main ()

{
    int dataset, directory, i, j, k;
    struct number phone_number[10000];
    scanf("%d", &dataset);
    for ( k = 0; k < dataset; k++ )
    {
        scanf("%d", &directory);
        for ( i = 0; i < directory; i++ )
        {
            scanf("%s", &phone_number[i].phone_num);
            for ()
        }
    }




}
